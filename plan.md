# 看板现代明亮视觉改造计划

## 1. 当前问题与设计目标


### 当前视觉问题

- 页面以深蓝背景、深色卡片和低对比边框为主，整体偏暗，内容区域的层次不够鲜明。
- 卡片、筛选区和明细表格使用相近的深色表面，分组关系主要依赖边框，浏览时重点不够突出。
- 正常、预警等状态主要依赖颜色文字和深色底色，信息密度较高时识别成本偏高。
- 标题、指标数值、辅助说明之间的字号和颜色层级仍可拉开，移动端空间紧张时可读性会下降。
- 悬停、焦点、弹窗等交互状态的视觉反馈不够完整，键盘操作的可见焦点需要强化。

### 设计目标

- 保留现有六个视图、筛选、指标卡下钻和明细表格的信息架构，改造为明亮、现代、可信赖的工作台风格。
- 通过背景、表面、边框、阴影和留白建立清晰层次，让关键数字和异常状态一眼可见。
- 保证中文文本、指标数值、表格和操作控件在桌面与窄屏下都具备稳定的可读性和可操作性。
- 让颜色不仅承担装饰作用，也能明确表达正常、预警、失败和交互反馈，并提供非颜色辅助。

## 2. 目标视觉规范

### 配色

- 页面背景：使用偏冷的浅灰蓝色（如 `#f4f7fb`），避免纯白大面积造成刺眼感。
- 主表面：卡片、筛选区、表格和弹窗使用白色或近白色（如 `#ffffff`），与页面背景形成明确对比。
- 文字层级：主标题使用深海军蓝（如 `#172033`），正文使用深灰蓝（如 `#344054`），辅助文字使用中灰蓝（如 `#667085`）。
- 品牌强调色：使用清晰但不过饱和的青绿色或蓝绿色（如 `#0f9f83`），用于活动标签、进度条和主要操作反馈。
- 状态色：正常使用绿色，预警使用琥珀色，失败使用红色；状态文字与浅色状态底同时使用，且不单靠颜色区分。

### 背景与卡片

- 顶部栏改为明亮的品牌渐变或白色主表面，保留轻微品牌色块以维持页面识别度。
- 筛选区与内容区使用白色卡片，采用细而低对比度的边框，并通过不同的圆角和轻阴影区分浮层关系。
- 指标卡保持网格布局；卡片顶部展示指标与状态，数值作为最强视觉焦点，趋势和进度条作为次级信息。
- 表格表头使用浅色底或细分隔线，行悬停时提供柔和背景反馈，避免大面积深色区域。

### 字体层级

- 延续系统字体栈和 `Noto Sans SC` 中文支持，避免引入额外字体资源。
- 页面标题约 28–32px，区块标题约 17–19px，指标数值约 26–30px。
- 正文保持 13–14px，标签、表头和状态徽标使用 11–12px；通过字重和颜色而非过多大写来建立层级。
- 保持约 1.45–1.6 的行高，长辅助文本和弹窗说明不压缩行距。

### 间距、圆角与阴影

- 以 4px 为基础间距单位，主要使用 8/12/16/24/32px，统一页面节奏。
- 卡片和表格容器使用 10–14px 圆角，控件使用 6–8px 圆角，避免圆角过度影响信息密度。
- 使用低透明度、较大模糊半径的浅色阴影（如 `0 6px 20px rgba(...)`）；普通卡片阴影轻，弹窗阴影明显。
- 保留足够的卡片内边距和区块间距，桌面端舒展、移动端不产生横向溢出。

### 状态与交互

- 正常、预警、失败状态使用“文字 + 徽标底色 + 必要时图标/文字说明”的组合。
- `hover` 使用边框或浅色背景变化，`focus-visible` 使用清晰的品牌色外描边，保证键盘用户能定位当前控件。
- 按钮、下拉框、标签页和可点击卡片提供一致的悬停、按下、禁用（如适用）反馈。
- 弹窗遮罩保持足够对比但不压暗内容；弹窗、关闭按钮和表格在键盘及窄屏下均可访问。

## 3. 页面、组件与改造范围

### `styles.css`

- 重构全局颜色变量、页面背景、顶部栏和主要文字层级。
- 调整 `.filters`、`.tabs`、`.card`、`.table-wrap`、`dialog` 等容器的表面、边框、圆角和阴影。
- 优化 `.value`、`.muted`、`.status`、`.bar` 等指标相关元素的对比度和层级。
- 增加卡片、标签页、按钮、下拉框、表格行和弹窗的 `hover`/`focus-visible` 状态。
- 细化 650px 以下响应式规则，确保筛选控件、标题、卡片网格和表格在小屏可用。

### `index.html`

- 保持现有信息架构和无障碍语义，必要时补充控件关联标签、按钮状态或可见焦点所需属性。
- 不改变现有筛选项、视图标签和指标弹窗的功能入口。

### `app.js`

- 保持示例数据、视图切换和下钻逻辑不变。
- 若视觉状态需要更明确的语义，仅补充必要的状态类名或可访问文本，不在本阶段扩展业务逻辑。

### `README.md`

- 在视觉改造完成后补充启动方式或界面说明（仅在现有说明不足以反映最终交付时更新）。

## 4. 分阶段实施步骤

### 阶段一：基线与视觉基础

1. 在现有页面上确认桌面端、窄屏端的布局基线和关键交互路径。
2. 建立颜色、间距、圆角、阴影和字体层级变量，先完成页面背景、顶部栏和基础文字更新。
3. 检查文字、表面和边框的对比度，确保浅色背景下信息不会变淡。

### 阶段二：核心内容组件

1. 改造筛选区、标签页和操作按钮，突出当前选择并统一控件状态。
2. 改造指标卡，强化数值焦点、状态徽标、趋势文字和进度条的层次。
3. 改造明细表格和弹窗，加入行悬停、分隔线、浮层层次以及合理的窄屏处理。

### 阶段三：响应式与无障碍

1. 在 650px 及以下宽度验证顶部栏、筛选区、卡片网格和表格的排列与滚动行为。
2. 补齐键盘焦点、弹窗关闭和可点击卡片的可见反馈，确认颜色之外仍有文字或结构提示。
3. 使用浏览器检查主要状态、长文本、空数据提示和高缩放场景。

### 阶段四：回归与交付

1. 验证六个视图、四个筛选项、刷新按钮、指标卡下钻和表格内容均保持可用。
2. 对比改造前后截图或浏览器预览，修正局部对比度、间距和溢出问题。
3. 更新必要文档，确认提交只包含视觉改造相关文件。

## 5. 验收标准

- **整体观感**：打开 `index.html` 后呈现明亮、现代、干净的工作台风格；背景、卡片、表格和弹窗具有清晰层次，不再以大面积深色为主。
- **信息可读性**：页面标题、指标数值、趋势、状态、表头和辅助说明具有明确层级；正常与预警指标可快速识别，文字与背景达到可读对比度。
- **功能保持**：总览及其他五个视图可切换，筛选和刷新后内容正常重绘，指标卡可打开并关闭定义弹窗。
- **交互反馈**：标签页、按钮、下拉框、卡片和表格行在悬停、聚焦和操作时有一致且明显的反馈；键盘可完成主要操作。
- **响应式表现**：桌面端网格充分利用空间；窄屏端筛选区可堆叠、卡片不被截断、表格可横向查看，页面无非预期横向溢出。
- **无障碍注意事项**：不以颜色作为唯一状态线索；保留语义化 HTML、`aria-label` 和 `aria-live` 行为；焦点指示清晰，弹窗内容和关闭操作可访问。
- **预览交付内容**：最终预览应包含浅灰蓝页面背景、明亮顶部栏、白色筛选/指标/表格卡片、青绿色品牌强调、绿色/琥珀/红色状态徽标、轻量阴影与完整焦点反馈，并覆盖桌面和移动宽度。

## 6. 风险、兼容性与验证方式

### 风险与兼容性

- 浅色表面若边框或辅助文字过淡，可能降低信息辨识度；需要以对比度检查和实际浏览器观察共同校准。
- 状态色在不同显示器和色觉条件下表现不同，因此必须保留“正常/预警/失败”等文字或结构线索。
- 依赖 CSS 新特性可能影响较旧浏览器；优先使用现有 CSS 能力和渐进增强，避免引入构建步骤或新依赖。
- 表格与弹窗的移动端布局可能增加滚动；应优先保证内容完整和操作可达，而不是强行压缩列宽。
- 颜色和阴影调整可能改变品牌识别；若有产品色值约束，应在实施阶段以现有品牌规范为准。

### 验证方式

1. 直接在现代浏览器打开 `index.html`，逐一操作视图、筛选、刷新、指标卡和弹窗。
2. 在至少一个桌面宽度和 650px 以下移动宽度检查布局、滚动、文本换行和焦点顺序。
3. 使用浏览器开发者工具检查键盘导航、焦点可见性、对比度和控制台错误。
4. 对比关键视图截图，确认明亮视觉方向、状态层级和信息密度符合上述验收标准。
5. 运行仓库已有的检查命令（如有）；本项目无构建步骤时，以浏览器手工回归作为主要验证手段。
